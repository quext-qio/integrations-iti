from aws_cdk import Duration


class GuestCard:
    @staticmethod
    def create_definition(self, stage_name, api_version, allow_methods, environment, timeout=Duration.seconds(900)):
        # --------------------------------------------------------------------
        # Create Required Layers
        

        pass
